# Ensure pnpm is used
package-manager=pnpm

# Disable frozen lockfile for Vercel deployment
frozen-lockfile=false

# Auto-install peer dependencies
auto-install-peers=true

# Use exact versions
save-exact=true 